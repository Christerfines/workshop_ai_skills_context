The company would like a new web portal for buying products, contacting support, tracking orders etc. 

This will be an application that we need built for demo purposes, but the idea is that participants in the workshop will be presented with 4 "support incindents" from different users (external customer, internal support teams and business). They will need to build a reusable skill for a developer to be able to use to investigate support issues. Each "support" case might contain long log files, random noise from discussion with customer and poor information about the actual issue. 

Right now I need a spec for the application that another LLM will build the application used in this workshop as reference for creating a reusable skill. 

Can you create a spec for such an application that is broken down into various parts of functionality, later the another LLM will build it and then we will introduce deliberate bugs and generate fake support indidents (but this will come later).

.NET, inmemory, run with "dotnet run", pref .NET 10 and API/Web as single project, should include logging (console), should have a nice web UI. 